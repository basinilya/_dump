{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "description": "A message returned to a client when the server cannot process a message.",

  "type": "object",
    
  "properties": {
    "errors": {
      "type": "array",
	  "minItems": 1,
      "items": {
        "type": "object",
        "properties": {
          "code": {
            "description": "a symbolic code identifying the error, for interpretation by machine",
            "type": "string",
            "minLength": 1,
            "maxLength": 32
          },
          "description": {
            "description": "a human-readable description of the error",
            "type": "string"
          },
          "elementref": {
            "description": "an RFC 6901 JSON Pointer identifying the invalid element",
            "$ref": "common.schema.json#/definitions/JsonPointer"
          }
        },
        "required": [
          "code",
          "description"
        ]
      }
    }
  },
  
  "required": [
    "errors"
  ]
}
